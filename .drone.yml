kind: pipeline
type: docker
name: default

steps:
- name: deliver
  image: docker:dind
  # volumes:
  # - name: dockersock
  #   path: /var/run
  commands:
  - export DOCKER_HOST=45.95.115.119
  - sleep 5 # give docker enough time to start
  - docker ps -a
  - docker run -d busybox
  - docker ps -a

# services:
# - name: docker
#   image: docker:dind
#   privileged: true
#   volumes:
#   - name: dockersock
#     path: /var/run

# volumes:
# - name: dockersock
#   temp: {}
